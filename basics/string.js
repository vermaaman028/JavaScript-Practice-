const name = "Aman verma"
const count = 14

console.log(`Hello my name is ${name} and my  repo count is ${count}` )

const user = new String ("verma aman")
console.log(user);

// learn string prototypes
